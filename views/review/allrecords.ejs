<h1 style="font-size:45px; font-family:Verdana, Geneva, Tahoma, sans-serif; color:white; font-style:italic; position: relative; left:5%">Reviews</h1>
<% if (allrecs.length > 0) {%>
    <table border="1">
    <tr>
    <th>Review ID</th>
    <th>Product ID</th>
    <th>Customer ID</th>
    <th>Rating</th>
    <th colspan="3">Actions</th>
    </tr>
    <% allrecs.forEach((recref, index)=> {%>
    <tr>
    <td><%= recref.review_id %></td>
    <td><%= recref.product_id %></td>
    <td><%= recref.customer_id %></a></td>
    <td><%= recref.rating %></a></td>
    <td><a  class = "button1" href='/review/<%= recref.review_id %>/show'> 
    Details</a></td>
    <td><a  class = "button1" href='/review/<%= recref.review_id %>/edit'>Edit</a></td>
    <td><a  class = "button1" href='/review/<%= recref.review_id %>/delete' 
    onclick="return confirm('Are you sure you want to delete this record?');">Delete</a></td>
    </tr>
    <% }); %>
    </table>
    <% } else { %>
    <p>Not Records Available</p>
    <% } %>
    <p> <a  class = "button1" href='/review/addrecord'>Add New</a> </p>